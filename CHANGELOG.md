# Changelog

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

暂无已记录变更。

### Planned
- Web 会话可换 Redis 以支持多进程部署
- 可选 WebSocket 推流，降低高频 step 轮询
- 桌面与 Web 统一可调动画步长

## [0.2.0] - 2026-08-18

### Added
- 新增 `heatlab-web-desktop` 启动器：用 PyInstaller 将 Flask 后端、模型、模板和静态资源封装为 Windows 一键演示版 `HeatLab-Web.exe`；启动后自动打开默认浏览器，GitHub Actions 产出 `HeatLab-Web-win64.zip`
- 热力学专题新增**等温 / 等压 / 等容**准静态过程模式：P-V 图叠加理论过程线，等容下压强由温度驱动
- 麦克斯韦专题新增**水平速度分量 v_x 高斯分布**（理论曲线 + 样本直方图），与速率 f(v) 双图对照，对齐任务文档「几率~水平速度曲线」
- 桌面版 UI 完全重写对齐 Web 工作台：VS Code Dark+ 配色、顶栏（品牌 + 种子命令托盘 + 运行簇）、专题 Tab、三栏「参数 | 场景 | 图表」、底部状态栏；全局暂停统一控制
- 热力学分子热运动场景升级为 **3D 透明盒体**：体积随 T/P 缩放、粒子按速率着色（慢=蓝 → 快=黄），桌面用 Matplotlib 3D、Web 用 Canvas 等轴投影
- 新增分量分布与过程约束的自动化测试（总数 13 → 21）
- 布朗运动场景升级：借鉴开源硬球模型为液体分子加入**分子间弹性碰撞**（等质量碰撞 + 位置分离），液体呈现为稠密流体而非穿模的独立粒子；花粉碰撞高亮记录在**花粉表面接触点**；Web 端液体分子**按速率着色**并带速度方向短线、花粉粒子加**表面颗粒斑点**；Web 与桌面均新增「轨迹方向箭头」「液体速度矢量」「轨迹渐隐」开关
- 布朗运动读数新增「花粉碰撞」「液体碰撞」计数；场景加图例（液体分子/轨迹/花粉粒子/碰撞高亮/运动方向）
- 热力学新增 **P-V / P-T / V-T 平面图**（大学物理常用等值线族），与 3D 相图同屏对照
- Web 端：3D 相图与每个平面图右上角**放大弹窗**（保留图注/轴/Plotly 工具栏）；步进时保留 3D 相机视角；滚轮缩放；登录页品牌徽标文字重设计
- Web 端：**默认不自动播放**动画，进入专题保持静态初始画面，手动触发；首次进入每个专题弹出**实验说明 + 使用教程**信息弹窗，公式用 **KaTeX** 渲染
- 伽尔顿板场景品质化：金属铁钉、顶部漏斗、底部狭槽与**六边形堆积**弹珠、粒子从漏斗内部逐钉弧线下落（修复凹槽上方振荡与跳过首钉）

### Changed
- Windows 发布路线改为 **Web + 本地后端 exe 优先**，不再在 `build-windows.yml` 中打包外观与功能较旧的 PySide6 合并窗口；PySide6 入口仅保留为源码对照
- Web 深色工作台改用更高对比度的蓝灰色表面与浅色文字，并放大标签、控件、图表刻度、图例和状态栏字号；理想气体场景在侧栏、场景角标和专题说明中明确标注“相对显示体积”
- 热力学相图恢复为 **3D P-V-T 相图**（文档要求「PV-T 相图：给出 (P,V,T) 坐标值」）；图上与参数区标注当前状态三元组坐标
- **移除意义不明的「动能论轨迹」**（每帧由 180 采样粒子估计压强产生的抖动曲线）；动能论压强保留为参数区只读指标
- **默认随机种子改为 `42`**
- **热力学 / 麦克斯韦分子热运动性能与物理观感优化**：
  - 显示速度系数从 0.5/0.55 提高到 **1.5**，粒子以热速率尺度活跃运动，不再“慢漂移”
  - 服务端对理想气体相图几何（PV=nRT 曲面 / 等值线族 / 过程线）做**签名缓存**：粒子每帧 step 时宏观状态不变 → 直接复用，不再每帧重算大数组
  - Web 端 3D 相图 + 平面图渲染**节流到每 4 帧**（约 130ms 一刷），粒子场景保持每帧更新；麦克斯韦温度步进每 3 帧刷新一次直方图
  - 麦克斯韦直方图采样从 6000 降到 3000，减轻高频 set 的开销
- 麦克斯韦分布图改为**线性数值轴**：温度升高时曲线真实右移、变宽、峰值变矮；y 轴上限固定在 0 °C 峰值（`y_max_fixed`），不再随坐标轴自动缩放而“变形”

### Fixed
- 图注特殊符号（上标/下标）改用 mathtext 渲染，消除缺字形方框乱码
- 修复 pyproject.toml 元数据（dependencies 误置于 project.urls），GitHub Actions 恢复通过
- 提交历史作者统一为仓库所有者本人
- 桌面 Matplotlib 图注/图例/坐标轴在深色背景下使用浅色文字，避免黑字看不清
- 图例文字改为纯白并放大到 11pt；优先使用常规字重中文字体（Droid Sans Fallback），避免细体小字在深色底上难以辨认

## [0.1.0] - 2026-08-08

### Added
- 四个热学专题数值模型：理想气体、布朗运动、麦克斯韦分布、伽尔顿板
- 桌面 GUI：合并窗口（`heatlab`）与四专题独立入口
- 浏览器实时实验台：Flask 会话引擎 + Vue 3 CDN + Canvas / Chart.js
- 全局种子派生的可复现随机流
- Web 工业扁平工作台 UI（Dark+ 风格）与 lab-console 顶栏（brand plate + 命令托盘 + 运行簇）
- 内置中文字体 `HeatLab CJK`（DroidSansFallback）
- 单元测试、数值验证脚本与示例输出图
- MIT 许可证与第三方声明

[Unreleased]: https://github.com/WALKERKILLER/heatlab/compare/v0.2.0...HEAD
[0.2.0]: https://github.com/WALKERKILLER/heatlab/releases/tag/v0.2.0
[0.1.0]: https://github.com/WALKERKILLER/heatlab/releases/tag/v0.1.0
